apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - cluster-issuer-letsencrypt.yaml
  - gateway-certificate.yaml
  # secret-cloudflare-api-token.placeholder.yaml intentionally excluded —
  # see comment in that file. Create the real Secret out-of-band.
