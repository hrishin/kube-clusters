apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: nvidia-gpu-operator
  namespace: gpu-operator
spec:
  releaseName: nvidia-gpu-operator
  chart:
    spec:
      chart: gpu-operator
      version: "v26.3.3"
      sourceRef:
        kind: HelmRepository
        name: nvidia-gpu-operator
  interval: 2m
  values:
    operator:
      defaultRuntime: containerd
      # Run the operator controller on core nodes
      nodeSelector:
        node-type: core
      tolerations:
        - key: node-type
          operator: Equal
          value: core
          effect: NoSchedule
    driver:
      enabled: true
      # driver installs to /run/nvidia/driver — DRA driver reads from the same path

    toolkit:
      enabled: true

    nfd:
      enabled: true

    # Device plugin disabled — GPU exposure is handled by the DRA driver instead
    devicePlugin:
      enabled: false

    # Tolerations applied to ALL GPU Operator DaemonSets (driver, toolkit,
    # dcgm-exporter, gpu-feature-discovery).
    daemonsets:
      tolerations:
        - key: nvidia.com/gpu
          operator: Exists
          effect: NoSchedule
        - key: workload
          operator: Equal
          value: inference
          effect: NoSchedule

    # NFD is included as a sub-chart. gc and master are Deployments that must
    # run on the core node. worker is a DaemonSet that must reach every node
    # (including mimir/logging/gpu) to discover hardware features.
    node-feature-discovery:
      gc:
        nodeSelector:
          node-type: core
        tolerations:
          - key: node-type
            operator: Equal
            value: core
            effect: NoSchedule
      master:
        nodeSelector:
          node-type: core
        tolerations:
          - key: node-type
            operator: Equal
            value: core
            effect: NoSchedule
      worker:
        tolerations:
          - key: node-type
            operator: Exists
            effect: NoSchedule
          - key: nvidia.com/gpu
            operator: Exists
            effect: NoSchedule
          - key: workload
            operator: Exists
            effect: NoSchedule
