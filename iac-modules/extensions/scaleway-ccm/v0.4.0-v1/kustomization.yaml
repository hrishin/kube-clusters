apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - repo.yaml
  - release.yaml
  # secret.yaml is intentionally excluded; create the scaleway-cloud-config
  # Secret manually or via ExternalSecret before installing the CCM.
